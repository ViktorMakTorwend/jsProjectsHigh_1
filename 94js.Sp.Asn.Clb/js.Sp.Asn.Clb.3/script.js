"use strict";
//⋕js.Sp.Asn.Clb.3


/* function loadImage (pathElement , callback){
	let image = document.createElement('img');
	image.src = pathElement;
	let loaded = false;
	let err;

	image.addEventListener('load', function() {
		loaded = true;
	});

	console.log(loaded)

	if(!loaded) {
		err = 'no pic in this way'
	}
	else {
		err = null;
	}

	callback(image, err);
}

loadImage ('img.png', function(image, err){
	if(!err){
		document.body.append(image)
	}
	else {
		console.log('an error by loading of the image ' + err);
	}
});
 */